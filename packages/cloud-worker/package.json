{
	"name": "@earendil-works/pi-cloud-worker",
	"version": "0.85.1",
	"private": true,
	"description": "Experimental cloud session worker: pi harness on PostgreSQL sessions and OpenSandbox execution",
	"type": "module",
	"scripts": {
		"start": "tsx --tsconfig ../../tsconfig.json src/main.ts",
		"smoke": "tsx --tsconfig ../../tsconfig.json src/smoke.ts",
		"test": "vitest --run"
	},
	"author": "Earendil Works",
	"license": "MIT",
	"engines": {
		"node": ">=22.19.0"
	},
	"dependencies": {
		"@earendil-works/pi-agent-core": "^0.85.1",
		"@earendil-works/pi-ai": "^0.85.1",
		"@earendil-works/pi-coding-agent": "^0.85.1",
		"@earendil-works/pi-env-opensandbox": "^0.85.1",
		"@earendil-works/pi-session-backend-postgres": "^0.85.1"
	},
	"devDependencies": {
		"vitest": "4.1.9"
	}
}
