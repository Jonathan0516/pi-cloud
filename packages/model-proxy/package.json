{
	"name": "@earendil-works/pi-model-proxy",
	"version": "0.85.1",
	"private": true,
	"description": "Experimental model proxy: holds vendor API keys, admits workers by session-bound tokens, meters usage",
	"type": "module",
	"scripts": {
		"start": "tsx --tsconfig ../../tsconfig.json src/main.ts",
		"test": "vitest --run"
	},
	"author": "Earendil Works",
	"license": "MIT",
	"engines": {
		"node": ">=22.19.0"
	},
	"dependencies": {
		"@earendil-works/pi-ai": "^0.85.1",
		"@earendil-works/pi-session-backend-postgres": "^0.85.1"
	},
	"devDependencies": {
		"vitest": "4.1.9"
	}
}
