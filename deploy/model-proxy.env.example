# Copy to deploy/model-proxy.env. Only the model proxy container reads this file.
# One line per provider the proxy should serve; see packages/model-proxy/src/config.ts for the names.
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
